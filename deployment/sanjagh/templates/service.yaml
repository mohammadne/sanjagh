apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanjagh.fullname" . }}
  labels:
    {{- include "sanjagh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sanjagh.selectorLabels" . | nindent 4 }}
